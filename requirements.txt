# Core
torch>=2.12.0
torchvision>=0.27.0

# Image processing
numpy>=2.0.0
opencv-python>=4.10.0
Pillow>=10.0.0
matplotlib>=3.9.0
PyTurboJPEG>=1.7.0,<2.0  # 2.x requires libjpeg-turbo 3.x; system ships 2.x

# Augmentation (training)
albumentations>=2.0.0

# Data
h5py>=3.10.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0

# Logging
tensorboard>=2.16.0

# ─────────────────────────────────────────────────────────────────────────────
# Optional — uncomment as needed (see README "Optional dependencies")
# ─────────────────────────────────────────────────────────────────────────────
# wandb>=0.16.0          # experiment logging
# fvcore>=0.1.5          # FLOPs measurement in benchmark
# thop>=0.1.1            # FLOPs measurement fallback
# onnx>=1.16.0           # ONNX export
# onnxsim>=0.4.36        # ONNX graph simplification
